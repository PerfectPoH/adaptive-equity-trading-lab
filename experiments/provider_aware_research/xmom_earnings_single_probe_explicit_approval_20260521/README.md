# XMOM earnings single-probe explicit approval

This directory records the explicit user approval for one bounded Intrinio earnings metadata probe.

The approval is limited to:

- provider: Intrinio
- symbol: CRMD
- endpoint: `companies/{identifier}/upcoming_earnings`
- max provider calls: 1
- raw payload retention: false

At approval recording time, no provider query, network call, market-data download, extractor implementation, backtest, or strategy promotion was performed.
