# Candidate 017 EODHD Micro-Probe Rerun 003 Gate

This gate authorizes exactly one bounded EODHD component probe after confirming `EODHD_API_KEY` is visible in the project `.env`.

The run may verify delisted list access, terminal delisted OHLCV access, and active adjusted EOD access. It may not retain raw payloads, build a dataset, run a backtest, run portfolio selection, run Kronos inference, promote a strategy, paper trade, or live trade.
