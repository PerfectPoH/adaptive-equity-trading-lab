# Intrinio access request note - 2026-05-18

Suggested request wording:

```text
For an equity research provider-sensitivity study, please enable historical end-of-day US equities data. Ideally this should include adjusted and unadjusted OHLCV, corporate actions/splits/dividends adjustment metadata, symbol/security master identifiers, delisting or inactive-security coverage where available, and coverage metadata by date.
```

Rationale:

```text
End-of-day history is sufficient for the current preregistered provider-sensitivity direction if it includes enough adjustment and coverage metadata to avoid survivorship and adjustment-policy ambiguity.
```

This note is informational only. It is not a provider query and does not activate or use Intrinio credentials.
