# XMOM earnings single probe output

Immutable output directory for one bounded earnings metadata probe.

Scope:

- provider: Intrinio
- symbol: CRMD
- endpoint: `companies/{identifier}/upcoming_earnings`
- max provider calls: 1
- raw payload retention: false

Before execution, this directory contains only pre-execution metadata.
