# Candidate 009 Regime Momentum Gate 001

This gate authorizes one post-autopsy diagnostic backtest.

It keeps only the `momentum_60d` sleeve and routes it only in `RECOVERY_BOUNCE` and `DRAWDOWN_STRESS`.

Because the route was derived from Candidate 008's same-panel autopsy, this run is diagnostic-only and cannot promote a strategy even if the numbers improve.
