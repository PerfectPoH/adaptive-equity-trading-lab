# Devlog - Provider sensitivity test spec - 2026-05-18

Created Provider Sensitivity Test spec for comparing old yfinance-based strategy outputs against Databento/Polygon evidence as a data-sensitivity check only.

```text
SPEC_ONLY_NOT_EXECUTED
OVERLAP_SELECTION_PREPARED
REDACTED_QUERY_PLAN_PREPARED
PROVIDER_QUERY_EXECUTED: no
READY_FOR_USER_REVIEW_BEFORE_EXECUTION
```

Reference old strategy: `breakout_continuation + open_to_close_return>=0.10 + IWM close > EMA200 + risk_fraction sizing`.
